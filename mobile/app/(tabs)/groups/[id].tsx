// Android-only: nested under the Groups tab stack so the tab bar stays visible and
// resettable (AndroidTabBar.tsx). iOS uses its own copy at group-details/[id].tsx instead.
export { default } from '@/components/GroupDetailsScreen';
